# AGENTS.md - つわりカウントダウンアプリ (tsuwarin) 開発ガイドライン

このファイルは、本プロジェクト（`tsuwarin`）における AI エージェントおよび開発者が遵守すべき指針、アーキテクチャ方針、コーディング規約を定めたものです。

---

## 1. プロジェクト概要
- **プロジェクト名**: つわりカウントダウンアプリ (`tsuwarin`)
- **目的**: 妊娠初期の辛いつわり（悪阻）に苦しむ妊婦さんが、出口の目安とされる「12週」「15週」までの残り時間を視覚的に把握し、精神的な支えと安心感を得られるようにするスマホ向けWebアプリケーション。
- **デプロイ先**: GitHub Pages（`main` ブランチのルート公開）
- **動作環境**: モバイルブラウザ（iOS Safari, Android Chrome）を第一ターゲットとしたレスポンシブWebアプリ。

---

## 2. 技術スタック & アーキテクチャ原則

### 2.1 ゼロビルド・ピュアWebスタック
- **HTML5**: セマンティックな構造、アクセシビリティ（a11y）配慮。
- **Vanilla CSS (CSS3)**: CSS Variables（カスタムプロパティ）を活用したデザインシステム構築。Tailwind CSSやCSS-in-JS、Sass等のトランスパイル・ビルドツールは使用しない。
- **Vanilla JavaScript (ES6+)**: フレームワーク（React, Vue等）やバンドラー（Vite, Webpack等）を用いず、ブラウザ標準機能のみで動作させる。

### 2.2 ファイル構成の分離
保守性・カスタマイズ性を最大化するため、**HTML・CSS・JavaScriptは必ず別ファイル**に分離して管理する。

```text
tsuwarin/
├── index.html         # アプリケーション構造・マークアップ
├── css/
│   └── style.css      # スタイル定義・デザインシステム
├── js/
│   ├── app.js         # メインロジック・イベントハンドリング
│   ├── calculator.js  # 週数・残り時間計算専用ロジック（純粋関数）
│   └── storage.js     # LocalStorage管理・永続化処理
├── assets/
│   └── icons/         # アイコン、ファビコン等（必要時）
├── AGENTS.md          # エージェント開発規約（本ファイル）
├── PRD.md             # プロダクト要求仕様書
└── DESIGN.md          # ビジュアルデザイン仕様書（カラー・UI/UX・タイポグラフィ）
```

> **Note**:
> - スタイリングおよびUIコンポーネント実装の際は、必ず [DESIGN.md](./DESIGN.md) に定義されたデザイントークン、カラーパレット、タイポグラフィ、コンポーネント仕様に厳密に従うこと。
> - JavaScriptは責務に応じて `calculator.js` や `storage.js` 等に適切に分割し、`type="module"` または `<script defer>` を用いて安全にロードすること。

---

## 3. GitHub Pages 運用の制約事項

1. **パスの指定**:
   - リポジトリ名を含むサブディレクトリ配下（`https://<username>.github.io/<repo>/`）でもルート（カスタムドメイン）でも正しく動作するよう、**リソースパスは常に相対パス（例: `./css/style.css`）**を使用すること。絶対パス（`/css/style.css`）は禁止。
2. **ビルドフリー**:
   - `main` ブランチにプッシュするだけで即時反映される構成を維持する。`dist/` や `build/` ディレクトリは作成しない。
3. **静的ホスティング**:
   - サーバーサイド処理は存在せず、すべての状態管理・ロジックはブラウザ（クライアントサイド）で完結させる。

---

## 4. コーディング規約 & 実装ルール

### 4.1 HTML / セマンティクス
- `<header>`, `<main>`, `<section>`, `<nav>`, `<footer>` 等のセマンティックタグを適切に使用する。
- フォーム部品（`<input>`, `<select>`, `<button>`）には必ず `id`、`name`、および関連する `<label>` または `aria-label` を付与する。
- スマートフォンのセーフエリア（Notch / Dynamic Island、ホームバー）を考慮した `<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">` を必ず設定する。

### 4.2 CSS / スタイリング
- **デザイントークンの集約**: カラー、フォントサイズ、余白、角丸、シャドウは `:root` 内の CSS カスタムプロパティ（CSS変数）として定義する。
- **モバイルファースト**: `min-width` または標準スタイルをモバイル向けに最適化し、PC/タブレット向けは必要に応じて `max-width` やメディアクエリでセンタリング等の調整を行う。
- **刺激の低減（つわり配慮）**:
  - 強い明度差、彩度が高すぎる蛍光色、激しい点滅・揺れのアニメーションは体調悪化を招く恐れがあるため厳禁。
  - 優しいパステルトーン（ウォームベージュ、ソフトピンク、ミントグリーン等）と滑らかなフェードイン等の微細なアニメーションを採用する。
- **タップ領域**: ボタンや入力UIのタップ可能領域は 44×44px 以上を確保する。

### 4.3 JavaScript / ロジック
- **純粋関数の分離**: 週数計算、残り時間計算などのコアロジックは、DOM操作から完全に切り離して単体テスト可能な純粋関数として実装する。
- **LocalStorage 安全策**:
  - Safari プライベートブラウズモード等での `localStorage.setItem` 例外に対応するため、必ず `try-catch` でラップする。
  - 保存データにはスキーマバージョンを含め、将来の拡張時に破損しないよう配慮する。
- **リアルタイム更新**:
  - 入力された週数・日数を「基準日時（入力時点のタイムスタンプ）」と紐付けて保存し、以後は現在日時との差分から自動でカウントダウンが進む設計とする（アプリを開き直しても時間が正しく進んでいること）。
  - `setInterval` または `requestAnimationFrame` による定期更新を行い、タブが非アクティブな状態から復帰した際（`visibilitychange` イベント）に再計算を行う。

---

## 5. エージェント向け開発ワークフロー & チェックリスト

エージェントがコードの追加・変更を行う際は、以下のチェックリストをすべて満たすこと。

- [ ] HTML, CSS, JS が正しく分離されているか
- [ ] 相対パス（`./...`）で記述され、GitHub Pagesで404にならないか
- [ ] 0週0日〜40週6日までの全境界値で計算エラー（NaN, 負の値, 不正な表示）が発生しないか
- [ ] 12週達成時（祝福＆15週切替）および15週超過時（カウントダウン停止＆現在週数のみ表示）が仕様通り動作するか
- [ ] 12週と15週の切り替えがスムーズに機能し、状態がLocalStorageに保持されるか
- [ ] LocalStorageが空の初期状態で、ウェルカム・オンボーディング画面が正しく表示されるか
- [ ] WCAG 2.1 AA準拠のコントラスト比（4.5:1以上）および暗所用ダークモード（Night Sanctuary）が考慮されているか
- [ ] iPhone SE等の小型スマホ表示で、ファーストビューからあふれずスクロール不要に収まっているか
- [ ] コンソールにエラーや警告が出力されていないか
