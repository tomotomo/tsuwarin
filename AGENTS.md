# AGENTS.md - つわりカウントダウンアプリ (tsuwarin) 開発ガイドライン

このファイルは、本プロジェクト（`tsuwarin`）における AI エージェントおよび開発者が遵守すべき指針、アーキテクチャ方針、コーディング規約、および動作検証手順を定めたものです。  
個別具体的な機能仕様やUIデザインは **[PRD.md](./PRD.md)** および **[DESIGN.md](./DESIGN.md)** を正とし、本ドキュメントはそれらに依存しない普遍的な開発基盤・運用原則を規定します。

---

## 1. プロジェクト概要 & 原則
- **プロジェクト名**: つわりカウントダウンアプリ (`tsuwarin`)
- **目的**: 妊娠初期のつわりに苦しむ妊婦さんが、出口までの残り時間を視覚的に把握し、精神的な支えと安心感を得られるようにするスマホ向けWebアプリケーション。
- **デプロイ先**: GitHub Pages（`main` ブランチのルート公開）
- **動作環境**: モバイルブラウザ（iOS Safari, Android Chrome）を第一ターゲットとしたレスポンシブWebアプリ。

---

## 2. 技術スタック & アーキテクチャ原則

### 2.1 ゼロビルド・ピュアWebスタック
- **HTML5**: セマンティックな構造、アクセシビリティ（a11y）配慮。
- **Vanilla CSS (CSS3)**: CSSカスタムプロパティを活用したデザインシステム。ビルドツールやプリプロセッサは使用しない。
- **Vanilla JavaScript (ES6+)**: フレームワークやバンドラーを用いず、ブラウザ標準機能（ES Modules含む）で動作させる。

### 2.2 ファイル構成の分離
保守性・カスタマイズ性を最大化するため、**HTML・CSS・JavaScriptは必ず別ファイル**に分離して管理する。

```text
tsuwarin/
├── index.html         # アプリケーション構造・マークアップ
├── css/
│   └── style.css      # スタイル定義・デザインシステム
├── js/
│   ├── app.js         # メインロジック・イベントハンドリング
│   ├── calculator.js  # 計算専用ロジック（純粋関数）
│   └── storage.js     # ストレージ永続化処理
├── assets/
│   └── icons/         # アイコン、ファビコン等（必要時）
├── AGENTS.md          # 開発規約・環境・検証手順（本ファイル）
├── PRD.md             # プロダクト要求仕様書（機能要件の正本）
└── DESIGN.md          # ビジュアルデザイン仕様書（UI/UX・スタイルの正本）
```

---

## 3. GitHub Pages 運用の制約事項

1. **パスの指定**:
   - サブディレクトリ配下でもカスタムドメインでも動作するよう、**リソースパスは常に相対パス（例: `./css/style.css`）**を使用すること。絶対パス（`/css/style.css`）は禁止。
2. **ビルドフリー**:
   - `main` ブランチにプッシュするだけで即時反映される構成を維持する。`dist/` や `build/` ディレクトリは作成しない。
3. **静的ホスティング**:
   - サーバーサイド処理は存在せず、すべての状態管理・ロジックはブラウザ（クライアントサイド）で完結させる。

---

## 4. コーディング規約

### 4.1 HTML / セマンティクス
- セマンティックタグ（`<header>`, `<main>`, `<section>`, `<footer>` 等）を適切に使用する。
- フォーム部品には必ず `id`, `name`, および関連する `<label>` または `aria-label` を付与する。
- スマートフォンのセーフエリアを考慮した `<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">` を設定する。

### 4.2 CSS / スタイリング
- スタイリングおよびコンポーネント実装は、必ず **[DESIGN.md](./DESIGN.md)** のデザイントークン・カラーパレット・ガイドラインに従うこと。
- モバイルファーストで記述し、タップ領域（44×44px以上）を確保する。
- 画面酔い・視覚疲労を防ぐため、急激な明滅や激しいアニメーションは禁止する。

### 4.3 JavaScript / ロジック
- コアロジック（計算等）はDOM操作から分離し、テスト可能な純粋関数として実装する。
- LocalStorageアクセスは、プライベートブラウズ等での例外を防ぐため `try-catch` で保護し、フォールバック機構を設ける。
- リアルタイム更新時は、タブ復帰イベント（`visibilitychange`）を監視して表示の同期を保つ。

---

## 5. ローカル動作検証手順 (Local Verification)

JavaScriptに ES Modules（`import`/`export`）を使用するため、ローカル検証時はローカルHTTPサーバーを経由して動作確認を行う。

### 5.1 ローカルサーバーの起動
macOS標準の Python HTTP サーバー（または任意の静的サーバー）をプロジェクトルートで起動する。

```bash
# プロジェクトルートで実行
python3 -m http.server 8000
```
起動後、ブラウザで `http://localhost:8000` にアクセスして検証を行う。

### 5.2 動作検証の観点
ブラウザの開発者ツール（DevTools）を活用し、以下の観点で検証すること。
1. **コンソール検証**: エラー、例外、読み込み失敗（404）が発生していないこと。
2. **レスポンシブ検証**: 小型スマートフォン（375px幅）から大画面まで、レイアウト崩れがないこと。
3. **ストレージ検証**: LocalStorage が空の初期状態、およびデータ保存・更新・リロード後の復元が正常に行われること。

---

## 6. エージェント向け開発ワークフロー & チェックリスト

エージェントがコードの追加・変更を行う際は、以下のチェックリストをすべて満たすこと。

- [ ] HTML, CSS, JS が正しく別ファイルに分離されているか
- [ ] 相対パス（`./...`）で記述され、GitHub Pagesで404にならないか
- [ ] **[PRD.md](./PRD.md) に定義されたすべての機能要件および境界値ケースを満たしているか**
- [ ] **[DESIGN.md](./DESIGN.md) に定義されたデザイントークン、UI仕様、アクセシビリティ基準を満たしているか**
- [ ] LocalStorageの初期状態（未設定時）およびデータ更新・復元が正しく処理されるか
- [ ] モバイル画面でレイアウト崩れや意図しないスクロールが発生していないか
- [ ] ブラウザコンソールにエラーや警告が出力されていないか
