# DESIGN.md - つわりカウントダウンアプリ (tsuwarin) ビジュアルデザイン仕様書

本ドキュメントは、つわりカウントダウンアプリ（`tsuwarin`）におけるビジュアルデザイン、UI/UX哲学、デザイントークン、コンポーネント仕様、およびモーションガイドラインを定義したものです。  
AIエージェントおよび開発者が実装する際、視覚的なブレを生じさせず、一貫した世界観と最適なユーザー体験を再現するためのデザイン基準となります。

---

## 1. デザインコンセプト & 世界観 (Design Concept)

### 1.1 コンセプト: 「穏やかな寄り添い (Gentle Sanctuary)」
- **背景と課題**: 悪阻（つわり）の症状が重い時期は、光やスマートフォンの画面を見るだけでも吐き気や頭痛、眼精疲労（いわゆる画面酔い）が誘発されます。
- **デザイン方針**:
  - **刺激の最小化 (Low-Stimulus UI)**: 彩度が高すぎる原色、強い明度差（真っ白 × 真っ黒）、激しいアニメーションを排除します。
  - **温もりと安心感**: 病院や薬のような冷たい無機質さを避け、赤ちゃんのぬくもりや柔らかい布を連想させるオーガニックで優しいパステルトーンを採用します。
  - **1秒ごとの前進を伝える視覚表現**: 苦しい時間を耐えている妊婦さんに対し、「確実にゴール（12週・15週）へと進んでいる」実感を穏やかに伝えます。

---

## 2. デザイントークン & カラーシステム (Color System)

### 2.1 パレット定義
自然界の光や植物、肌のぬくもりをモチーフとした低彩度・高明度の調和色で構成します。

| トークン名 | HEXコード | 用途・心理的効果 |
| :--- | :--- | :--- |
| **`--bg-base`** | `#FAF7F2` | **アプリ全体の背景**: 眩しさを抑えたクリームアイボリー。真っ白（`#FFFFFF`）による眼精疲労を防ぎます。 |
| **`--bg-surface`** | `#FFFFFF` | **カード・モーダル背景**: 清潔感のある白。背景色との間にごくわずかなコントラストを作ります。 |
| **`--bg-soft`** | `#F3EFEA` | **入力欄背景・非アクティブ領域**: 柔らかいグレージュ。境界線を強調しすぎずに領域を区切ります。 |
| **`--color-primary`** | `#E07A5F` | **メインアクセント**: 落ち着きと血色感のあるテラコッタピンク。母性的な温もりを表現します。 |
| **`--color-primary-light`** | `#F9EAE1` | **選択タブ・バッジ背景**: テラコッタの淡色。アクティブ状態をやわらかく示します。 |
| **`--color-secondary`** | `#81B29A` | **進捗・達成・リフレッシュ**: 鎮静作用のあるセージミント。心拍を落ち着かせる癒やしの色です。 |
| **`--color-secondary-light`**| `#E8F2EC` | **プログレスバー背景**: セージの極淡色。 |
| **`--color-accent`** | `#F2CC8F` | **ハイライト・お祝い**: 温かいカスタードゴールド。星や達成時のワンポイント。 |
| **`--text-primary`** | `#3D405B` | **主要テキスト**: 柔らかなディープネイビーグレー。純黒（`#000`）特有のきついコントラストを避けます。 |
| **`--text-secondary`** | `#7D8597` | **補足テキスト・単位**: 視認性を保ちつつ主張しすぎないミディアムスレートグレー。 |
| **`--border-subtle`** | `#EBE4DB` | **境界線**: 背景に溶け込む極薄のウォームグレー。 |

### 2.2 CSS カスタムプロパティ定義 (`css/style.css` の `:root`)
```css
:root {
  /* Colors */
  --bg-base: #FAF7F2;
  --bg-surface: #FFFFFF;
  --bg-soft: #F3EFEA;
  --color-primary: #E07A5F;
  --color-primary-light: #F9EAE1;
  --color-primary-hover: #D46B50;
  --color-secondary: #81B29A;
  --color-secondary-light: #E8F2EC;
  --color-accent: #F2CC8F;
  --text-primary: #3D405B;
  --text-secondary: #7D8597;
  --border-subtle: #EBE4DB;

  /* Shadows */
  --shadow-sm: 0 2px 8px rgba(61, 64, 91, 0.04);
  --shadow-md: 0 4px 16px rgba(61, 64, 91, 0.08);
  --shadow-lg: 0 10px 30px rgba(61, 64, 91, 0.12);

  /* Radii (角丸) */
  --radius-sm: 8px;
  --radius-md: 14px;
  --radius-lg: 20px;
  --radius-full: 9999px;

  /* Spacing */
  --space-xs: 4px;
  --space-sm: 8px;
  --space-md: 16px;
  --space-lg: 24px;
  --space-xl: 32px;
}
```

---

## 3. タイポグラフィ (Typography)

### 3.1 フォントスタック
端末に最適化され、可読性の高いシステムフォントを優先します。
```css
font-family: -apple-system, BlinkMacSystemFont, "Hiragino Sans", "Hiragino Kaku Gothic ProN", "BIZ UDPGothic", "Yu Gothic", sans-serif;
```

### 3.2 等幅数字の必須化 (`tabular-nums`)
時間や日数がカウントダウン・更新された際に、数字の横幅差異によって文字が左右にガタつく現象（視覚的チラつき）を防ぐため、**すべての数値表示要素に `font-variant-numeric: tabular-nums;` を必ず適用**します。

### 3.3 タイプスケール & ウェイト
| レベル | フォントサイズ | Line-height | Weight | 主な適用箇所 |
| :--- | :--- | :--- | :--- | :--- |
| **Display (巨大数値)** | `38px〜44px` | 1.1 | 700 (Bold) | メインカードの残り時間（例: `432`） |
| **Title (見出し)** | `20px〜22px` | 1.3 | 700 (Bold) | アプリタイトル、カード主要テキスト |
| **Body Large (強調本文)** | `17px〜18px` | 1.5 | 600 (SemiBold)| 指定フォーマット「W週D日 目標週目まであと...」 |
| **Body (標準本文)** | `15px〜16px` | 1.6 | 400 (Regular) | 励ましメッセージ、説明文、ラベル |
| **Caption (注記・単位)**| `12px〜13px` | 1.4 | 500 (Medium) | 「時間」「日」などの単位、バッジ、サブ情報 |

---

## 4. レイアウト & レスポンシブ設計 (Layout Architecture)

### 4.1 モバイルファースト・ビューポート制約
- **最大幅制限**: スマートフォンでの閲覧（ベッド上や片手操作）に最適化するため、メインコンテンツコンテナの幅は **`max-width: 440px`** に制限し、PC・タブレット表示時は画面中央に配置します。
- **セーフエリア対応**: iPhoneのNotch/Dynamic Islandおよびホームバーを考慮します。
  ```css
  padding-top: max(16px, env(safe-area-inset-top));
  padding-bottom: max(24px, env(safe-area-inset-bottom));
  ```

### 4.2 画面構造図
```text
+------------------------------------------+
|  Header (ロゴ・タイトル & 設定アイコン)     |
+------------------------------------------+
|  Target Switcher (12週まで / 15週まで)     |
+------------------------------------------+
|                                          |
|  Hero Countdown Card                     |
|  +------------------------------------+  |
|  | [現在 9週3日] (ピル型バッジ)        |  |
|  |                                    |  |
|  | あと 432 時間                       |  |
|  | (18日)                             |  |
|  |                                    |  |
|  | 9週3日 12週目まであと432時間(18日)   |  |
|  +------------------------------------+  |
|                                          |
+------------------------------------------+
|  Progress Card (進捗バー & パーセント)     |
+------------------------------------------+
|  Encouragement Card (寄り添いの一言)      |
+------------------------------------------+
|  Footer (週数の再入力・リセットリンク)     |
+------------------------------------------+
```

---

## 5. UIコンポーネント詳細仕様 (Components)

### 5.1 ヘッダー (App Header)
- **構成**:
  - 左側: やわらかなロゴアイコン（小さな双葉・ハートモチーフ）+ アプリ名「つわりカウントダウン」
  - 右側: 設定ボタン（ペンシル/歯車アイコン、タップ領域 `44px × 44px`）
- **スタイル**: 背景は透過、境界線はなし。すっきりとした余白（`margin-bottom: var(--space-md)`）。

### 5.2 ターゲット切り替えタブ (Target Switcher)
- **形式**: iOS風のセグメントコントロール（ピル型）。
- **ビジュアル**:
  - 全体枠: 背景 `--bg-soft`、角丸 `--radius-full`、内側パディング `4px`。
  - アイテム: `12週まで` / `15週まで`。
  - **アクティブ状態**: 背景 `--bg-surface`、文字色 `--color-primary`、ソフトなシャドウ `--shadow-sm`。
  - **非アクティブ状態**: 背景透明、文字色 `--text-secondary`。
  - 切り替え時: `transition: all 0.25s ease` で滑らかに移動。

### 5.3 メインカウントダウンカード (Hero Countdown Card)
アプリの中心となる最重要コンポーネントです。

- **外観**:
  - 背景: `--bg-surface`（純白）
  - 角丸: `--radius-lg` (20px)
  - パディング: `24px 20px`
  - 枠線: `1px solid var(--border-subtle)`
  - シャドウ: `--shadow-md`
- **内部要素の階層**:
  1. **現在週数バッジ**:
     - ピル型背景 `--color-primary-light`、文字色 `--color-primary`、太字 `13px`。
     - 表記例: `🌱 現在 9週3日`
  2. **特大カウントダウン数字**:
     - 表記例: `あと 432 時間`（数字部分は `40px`、単位は `16px`）
     - 色: `--text-primary`、等幅数字。
  3. **日数サブ表記**:
     - 表記例: `(約 18日)`
     - 色: `--text-secondary`、`16px`。
  4. **指定必須フォーマット表示枠**:
     - 背景: `--bg-soft`、角丸 `--radius-md`、内側パディング `12px 14px`。
     - **テキスト表示ルール**:
       ```text
       9週3日 12週目まであと432時間(18日)
       ```
       （※PRD改訂に基づき、読点ではなく半角スペース区切りで自然に読めるレイアウト）
     - フォント: `15px`、フォントウェイト `600`、色 `--text-primary`。

### 5.4 プログレスバー (Progress Bar & Milestones)
- **バー外枠**:
  - 幅: 100%
  - 高さ: `14px`
  - 角丸: `--radius-full` (丸みのあるカプセル形状)
  - 背景色: `--color-secondary-light` (`#E8F2EC`)
- **進捗フィル (Fill)**:
  - グラデーション: `linear-gradient(90deg, #A7D7C5 0%, #81B29A 100%)`
  - 角丸: `--radius-full`
  - 幅: 計算されたパーセント（`transition: width 0.6s cubic-bezier(0.4, 0, 0.2, 1)`）
- **プログレス情報テキスト**:
  - バー上部に `0週` と `目標(12週/15週)` のラベルを左右配置。
  - 右側に大きく太字で `78.5%` を表示。

### 5.5 寄り添い・励ましカード (Encouragement Card)
- **外観**:
  - 背景: `rgba(249, 234, 225, 0.6)`（薄いピーチトーンの透過）
  - 左端に優しいアクセントボーダー（`3px solid var(--color-primary)`）
  - パディング: `16px`
  - 角丸: `--radius-md`
- **テキスト**:
  - メッセージ例: `「今日も1日、本当によく頑張っています。赤ちゃんもママと一緒に成長していますよ。」`
  - 色: `--text-primary`、フォントサイズ `14px`、行高 `1.6`。

### 5.6 達成時ステート (Milestone Reached State)
目標週数（12週または15週）に達した際の特別なビジュアルです。

- **メインカードの変容**:
  - 背景: 上品な淡いイエロー・ミントのグラデーション
  - アイコン: お祝いのブーケ (`💐`) や星 (`✨`)
  - **メッセージ表示**:
    ```text
    12週目を迎えました！本当にお疲れ様でした💐体調はいかがですか？
    ```
  - アクションボタン:
    - 12週達成時: `「15週（安定期）までのカウントに切り替える」`（大型のピルボタン）

### 5.7 設定・入力モーダル (Settings / Input Modal)
- **形式**: 画面下部から滑らかにせり上がるボトムシート（Bottom Sheet）または中央モーダル。
- **背景オーバーレイ**: `rgba(61, 64, 91, 0.4)` + 軽いぼかし `backdrop-filter: blur(4px)`。
- **入力UI**:
  - 週数セレクト（`0週` 〜 `40週`）および 日数セレクト（`0日` 〜 `6日`）。
  - つわりで指先が震えたりだるくても押しやすい**大きなセレクトボックス（高さ `52px`、フォント `18px`）**。
- **CTAボタン**:
  - 幅 100%、高さ `52px`、角丸 `--radius-full`。
  - 背景 `--color-primary`、文字色白、太字 `16px`。
  - タップ時の心地よい押し込みフィードバック（`transform: scale(0.98)`）。

---

## 6. アニメーション & インタラクション基準 (Motion Guidelines)

1. **画面酔いを起こさないソフトイージング**:
   - 急激なバウンスや回転、明滅は厳禁。
   - すべてのトランジションには `cubic-bezier(0.25, 1, 0.5, 1)`（自然な減速曲線）または `ease-out` を使用します。
2. **遷移時間**:
   - モーダルの開閉: `0.3s`
   - タブ切り替え: `0.2s`
   - プログレスバー伸長: `0.6s`
3. **アクセシビリティ配慮 (`prefers-reduced-motion`)**:
   - ユーザーが端末設定で視覚効果を減らす設定にしている場合、すべてのアニメーション時間を `0.01ms` に短縮し、フェードのみで切り替えます。
   ```css
   @media (prefers-reduced-motion: reduce) {
     *, *::before, *::after {
       animation-duration: 0.01ms !important;
       transition-duration: 0.01ms !important;
     }
   }
   ```

---

## 7. 実装チェックリスト（ビジュアルデザイン基準）

AIエージェントが実装を完了する際、以下の項目を目視・コード上で確認すること。

- [ ] 背景色がまぶしい白（`#FFF`）ではなく、穏やかなアイボリー（`#FAF7F2`）になっているか
- [ ] テキストが強すぎる黒（`#000`）ではなく、スレートグレー（`#3D405B`）になっているか
- [ ] 数字表示部分に `font-variant-numeric: tabular-nums` が効いており、桁数の変化でガタつかないか
- [ ] メイン表記がPRD仕様通り `W週D日 {目標週}週目まであとh時間(d日)` と表示されているか
- [ ] タップターゲット（ボタン、タブ、入力欄）がすべて `44px × 44px` 以上の領域を持っているか
- [ ] iPhone Safari の上下セーフエリアでコンテンツが被らないか
- [ ] 12週／15週の切り替えタブが視覚的にわかりやすく、アクティブ状態が明瞭か
- [ ] 目標達成時に指定のメッセージ（`12週目を迎えました！本当にお疲れ様でした💐体調はいかがですか？`）が表示されるか
